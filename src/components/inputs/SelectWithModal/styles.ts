import styled from "styled-components";

interface IStyledSearchUserCard {
  isActive: boolean;
  smallScreen: boolean;
}

const StyledSearchUserCard = styled.div<IStyledSearchUserCard>``;

export { StyledSearchUserCard };
