import styled from "styled-components";
import { inube } from "@inube/design-system";

const StyeledConatiner = styled.div`
  display: flex;
  flex-direction: column;
  position: relative;
  gap: ${inube.spacing.s400};
`;

export { StyeledConatiner };
