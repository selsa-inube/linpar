import styled from "styled-components";
import { inube } from "@inube/design-system";

const StyledPrimaryFormContainer = styled.div`
  padding: ${inube.spacing.s600};
`;

export { StyledPrimaryFormContainer };
